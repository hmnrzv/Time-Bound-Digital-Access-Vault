# Time-Bound Digital Access Vault
Project
This project uses Django, HTML and CSS.
It allows multiple users to create accounts.
Users can create their own vaults, other users can not view those vaults unless a user allows them by specifying rules.
